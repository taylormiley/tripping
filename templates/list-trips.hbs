{{#each this}}
  <div>
    {{location}}
  </div>
  <div>
    {{#each reviews}}
      <span>{{title}}</span> | <span>{{date}}</span>
    {{/each}}
  </div>

  <button id="add-review#{{@key}}">Add Review</button>
{{/each}}